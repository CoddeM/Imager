<?xml version="1.0" encoding="utf-8"?>
<!--
  API 24-25 fallback. Adaptive icons need API 26, so on older releases every weekday alias just
  reuses the single raster launcher icon; the colour does not change there.
-->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@mipmap/ic_launcher"
    android:inset="0dp" />
